/**
 * File containing the colors used in the website.
 */

const docStyle = getComputedStyle(document.documentElement);

const green = docStyle.getPropertyValue('--green-emphasis');
const gray = docStyle.getPropertyValue('--gray-emphasis');
const yellow = docStyle.getPropertyValue('--yellow-emphasis');
const white = 'white';
