/**
 * File containing the numerical constants used in the website.
 */

/**
 * Maximum number of bits for the input.
 */
const MAX_NUM_BITS = 16;

/**
 * Arbitrarily large number used for skipping to the last step (show all routine).
 *
 * The value of this constant should be greater than the largest possible number of steps
 * that can result from applying any of the multiplication algorithms.
 */
const LARGE_STEP = 150;
